const { colors } = require('tailwindcss/defaultTheme');
module.exports = {
  theme: {
    colors: {
      primary: colors.teal,
      secondary: colors.orange,
      neutral: colors.gray,
      white: '#F2F2F2',
      lime: '#41F137',
      lightLime: '#6CF266',
      black: '#0E0E0E'
    },
    extend: {},
  },
  variants: {},
  plugins: [],
};
